---
// =============================================================================
// INDIVIDUAL BLOG POST PAGE - Single blog post using Astro Content Collections
// =============================================================================

import Layout from '@/layouts/BaseLayout.astro';
import { getCollection, render } from 'astro:content';
import { format } from 'date-fns';

// Generate static paths for all blog posts
export async function getStaticPaths() {
  const posts = await getCollection('blog');
  return posts.map(post => ({
    params: { slug: post.slug },
    props: { post },
  }));
}

const { post } = Astro.props;
const { Content } = await render(post);

// Format dates
const formattedDate = format(post.data.published_at, 'MMMM d, yyyy');

// Build social sharing data
const shareUrl = `${Astro.url.origin}/blog/${post.slug}`;

// Structured data for article
const structuredData = {
  "@context": "https://schema.org",
  "@type": "BlogPosting",
  "headline": post.data.title,
  "description": post.data.excerpt,
  "url": shareUrl,
  "datePublished": post.data.published_at.toISOString(),
  "author": {
    "@type": "Person",
    "name": "Sebastian Alexis",
  },
  ...(post.data.featured_image_url && {
    "image": {
      "@type": "ImageObject",
      "url": post.data.featured_image_url,
    }
  }),
  ...(post.data.reading_time && { "timeRequired": `PT${post.data.reading_time}M` }),
};
---

<Layout
  title={post.data.title}
  description={post.data.excerpt || `Read about ${post.data.title} on my blog.`}
  image={post.data.featured_image_url}
  structuredData={structuredData}
  type="article"
>
  <article class="min-h-screen">

    <!-- Hero Section -->
    <header>
      <div class="max-w-4xl mx-auto px-4 py-16 sm:py-24">

        <!-- Breadcrumb -->
        <nav class="mb-12" aria-label="Breadcrumb">
          <ol class="flex items-center space-x-2 text-sm font-light text-text-secondary">
            <li><a href="/" class="hover:text-text-primary">Home</a></li>
            <li>/</li>
            <li><a href="/" class="hover:text-text-primary">Blog</a></li>
          </ol>
        </nav>

        <!-- Title -->
        <h1 class="text-4xl md:text-6xl font-light tracking-tight text-text-primary leading-tight mb-8">
          {post.data.title}
        </h1>

        <!-- Excerpt -->
        {post.data.excerpt && (
          <p class="text-xl font-light text-text-secondary leading-relaxed mb-12">
            {post.data.excerpt}
          </p>
        )}

        <!-- Post Meta -->
        <div class="text-base font-light text-text-secondary mb-12">
          <span>{formattedDate}</span>
          {post.data.reading_time && (
            <span> • {post.data.reading_time} min read</span>
          )}
        </div>

        <!-- Featured Image -->
        {post.data.featured_image_url && (
          <figure class="mb-16">
            <img
              src={post.data.featured_image_url}
              alt={post.data.title}
              class="w-full h-auto rounded-lg"
              loading="eager"
              decoding="async"
            />
          </figure>
        )}

      </div>
    </header>

    <!-- Article Content -->
    <div class="max-w-4xl mx-auto px-4">
      <div class="mb-16">
        <div class="prose prose-lg prose-site prose-headings:font-light prose-headings:tracking-tight max-w-none">
          <Content />
        </div>
      </div>
    </div>

    <!-- Back to Blog -->
    <div class="max-w-4xl mx-auto px-4 py-16">
      <div class="border-t border-border-primary pt-8">
        <a
          href="/"
          class="inline-flex items-center text-text-secondary hover:text-text-primary transition-colors"
        >
          <svg class="w-4 h-4 mr-2" fill="none" stroke="currentColor" viewBox="0 0 24 24">
            <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M15 19l-7-7 7-7" />
          </svg>
          Back to all posts
        </a>
      </div>
    </div>

  </article>
</Layout>

<style is:global>
  .prose-site {
    color: var(--text-primary);
  }

  .prose-site h1,
  .prose-site h2,
  .prose-site h3,
  .prose-site h4,
  .prose-site h5,
  .prose-site h6 {
    color: var(--text-primary);
  }

  .prose-site a {
    color: var(--text-primary);
    text-decoration: underline;
  }

  .prose-site strong {
    color: var(--text-primary);
  }

  .prose-site blockquote {
    color: var(--text-secondary);
    border-left-color: var(--border-primary);
  }

  .prose-site img {
    @apply rounded-lg;
  }

  .prose-site code {
    color: var(--text-primary);
    background-color: var(--bg-tertiary);
    border: 1px solid var(--border-secondary);
    @apply px-1 py-0.5 rounded;
  }

  .prose-site pre {
    background-color: var(--bg-tertiary);
    border: 1px solid var(--border-secondary);
    @apply rounded-lg overflow-x-auto;
  }

  .prose-site pre code {
    @apply bg-transparent px-0 py-0;
    border: none;
  }

  .prose-site hr {
    border-color: var(--border-primary);
  }
</style>
